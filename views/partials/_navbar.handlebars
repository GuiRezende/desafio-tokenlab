<nav class="navbar navbar-expand-lg navbar-dark bg-dark .bg-dark.bg-gradient">
    <div class="container-fluid">
        <div class="collapse navbar-collapse justify-content-md-start ms-5" id="navbarSupportedContent">
            <a class="navbar-brand ms-2" href="/">TokenLab - Eventos</a>
        </div>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {{#if user}}
        <div class="collapse navbar-collapse mx-auto " id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="/admin/eventos">Eventos</a>
                </li>
            </ul>
        </div>
        <div class="collapse navbar-collapse justify-content-md-end me-5" id="navbarSupportedContent">
            <a href="/usuarios/logout" class="nav-link"><button class="btn btn-outline-light d-flex"
                    type="submit">Sair</button></a>
        </div>
        {{else}}
        <div class="collapse navbar-collapse justify-content-md-end me-5" id="navbarSupportedContent">
            <a href="/usuarios/login" class="nav-link"><button class="btn btn-outline-light d-flex"
                    type="submit">Login</button></a>
        </div>
        {{/if}}
    </div>
</nav>